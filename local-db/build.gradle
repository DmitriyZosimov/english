apply plugin: 'java'

dependencies {
    implementation spring.context,
            spring.jdbc,
            postgresql.postgresql

    testImplementation spring.test,
            junit.junit,
            easymock.easymock
}

tasks.named('test') {
    useJUnitPlatform()
}
